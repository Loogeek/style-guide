#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/long.xiao/Documents/Github/eslint-config/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/long.xiao/Documents/Github/eslint-config/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../registry.npmmirror.com+resolve@2.0.0-next.4/node_modules/resolve/bin/resolve" "$@"
else
  exec node  "$basedir/../../../../../registry.npmmirror.com+resolve@2.0.0-next.4/node_modules/resolve/bin/resolve" "$@"
fi
